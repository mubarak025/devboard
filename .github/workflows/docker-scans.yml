# Goal - Ensure the docker files and Docker images are security tested 

name: docker scan

on: 
  workflow_call:

jobs:
  docker-file-lint-and-scan:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        folder:
          - backend
          - frontend
        
    steps:
      - name: checkout code
        uses: actions/checkout@v7

      - name: ${{ matrix.folder }}/Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with: 
         dockerfile: ${{ matrix.folder }}/Dockerfile

      - name: Docker login
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker build ${{ matrix.folder }}
        run: docker build -t ${{vars.DOCKERHUB_USERNAME}}/devboard-${{ matrix.folder }}:latest .
        working-directory: ${{ matrix.folder }}

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{vars.DOCKERHUB_USERNAME}}/devboard-${{ matrix.folder }}:latest 
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
         
  